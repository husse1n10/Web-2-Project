APP_NAME="E-Services Platform"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Local: use mysql. Heroku: set DB_CONNECTION=pgsql (DATABASE_URL is auto-provided by Heroku Postgres addon)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=eservices
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_EXPIRE_ON_CLOSE=false
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=false
SESSION_SAME_SITE=lax

CACHE_STORE=database
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
QUEUE_FAILED_TABLE=failed_jobs

# Keep "log" by default. Set "pusher" after configuring Pusher keys.
BROADCAST_CONNECTION=log

# Mail (Mailtrap sandbox for development)
MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=your_mail_username
MAIL_PASSWORD=your_mail_password
MAIL_FROM_ADDRESS=noreply@eservices.gov.lb
MAIL_FROM_NAME="${APP_NAME}"
MAIL_EHLO_DOMAIN=127.0.0.1

# Google OAuth (Socialite)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/auth/google/callback

# GitHub OAuth (Socialite)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=http://127.0.0.1:8000/auth/github/callback

# Google Maps
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Azure Document Intelligence (ID extraction)
AZURE_DOC_INTELLIGENCE_ENDPOINT=
AZURE_DOC_INTELLIGENCE_KEY=
AZURE_DOC_INTELLIGENCE_API_VERSION=2024-11-30
AZURE_DOC_INTELLIGENCE_TIMEOUT=20
AZURE_DOC_INTELLIGENCE_POLL_ATTEMPTS=15
AZURE_DOC_INTELLIGENCE_POLL_INTERVAL_MS=900

# OCR.Space (free OCR fallback, no cloud subscription required)
OCR_SPACE_ENDPOINT=https://api.ocr.space/parse/image
OCR_SPACE_API_KEY=
OCR_SPACE_LANGUAGE=auto
OCR_SPACE_ENGINE=2
OCR_SPACE_TIMEOUT=20

# Stripe payments
STRIPE_KEY=pk_test_your_stripe_public_key
STRIPE_SECRET=sk_test_your_stripe_secret_key

# NOWPayments (crypto gateway — sandbox)
# Sign up at https://account.nowpayments.io and grab a sandbox API key + IPN secret.
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=
NOWPAYMENTS_BASE_URL=https://api.sandbox.nowpayments.io/v1

# SMS reminders (Twilio)
# Use "log" for local dry-run, "twilio" for real SMS.
SMS_DRIVER=log
SMS_DEFAULT_COUNTRY_CODE=+961
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=

# Groq AI chatbot (free tier — get a key at https://console.groq.com)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Pusher (real-time notifications)
PUSHER_APP_ID=your_pusher_app_id
PUSHER_APP_KEY=your_pusher_app_key
PUSHER_APP_SECRET=your_pusher_app_secret
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Frontend env vars (Vite)
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
